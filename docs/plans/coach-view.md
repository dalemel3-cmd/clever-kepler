# Coach View (parked, not built)

Decided with the head of S&C on 2026-09-26; parked until the current cleanup pass is done.

- **S&C staff view** keeps everything nuanced: A:C ratio, AU, sRPE, RSI, raw numbers.
- **Coach view** keeps the same layout and dashboard but uses plain language:
  - A:C ratio -> "Load spike" / "Normal" (color only)
  - AU -> hidden, color instead
  - sRPE -> "Effort 7/10"
  - Baseline -> "Normal weight"
  - RSI -> "Reactive jump"
  - Green / yellow / red flags instead of numbers
- The coach view shows only Today, Teams and Performance (Quick Hits + Print Report).
- No layout or visual redesign: same sidebar, cards, colors, fonts.
- Open question: how the app knows it's a coach (Settings switch, account role, or the
  walk-up iPad always showing the coach view).
- Mockup: https://claude.ai/artifact/HztmrhGTQfLzvtFarXxGSk
